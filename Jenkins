pipeline {
    agent any   // runs on any available node/slave

    stages {
        stage('Stage 1') {
            steps {
                echo 'Hello from Stage 1!'
                sh 'echo Running build step on slave/agent...'
            }
        }
    }
}
