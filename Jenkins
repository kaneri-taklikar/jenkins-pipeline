pipeline {
    agent {label 'slave'}   // runs on any available node/slave

    stages {
        stage('checkout') {
            steps {
                echo 'Hello from Stage 1!'
                sh 'echo Running build step on slave/agent...'
            }
        }
    }
}
